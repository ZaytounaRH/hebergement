<form method="post" action="{{ path('app_employee_delete', {'employee_id': employee.employee_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.employee_id) }}">
    <button class="btn">Delete</button>
</form>
