<form method="post" action="{{ path('app_certification_delete', {'idCertif': certification.idCertif}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certification.idCertif) }}">
    <button class="btn">Delete</button>
</form>
