{% extends 'back.html.twig' %}

{% block title %}Nouveau Congé{% endblock %}

{% block content %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2 class="text-center text-success mb-4">➕ Nouveau Congé</h2>

            <div class="card border rounded-4 shadow-sm p-4 bg-white">
                {{ include('conge/_form.html.twig', {'button_label': 'Créer'}) }}
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_conge_indexback') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}