{% extends 'base.html.twig' %}

{% block title %}Candidat{% endblock %}

{% block body %}
    <h1>Candidat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Candidat_id</th>
                <td>{{ candidat.candidatId }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ candidat.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_candidat_index') }}">back to list</a>

    <a href="{{ path('app_candidat_edit', {'candidat_id': candidat.candidat_id}) }}">edit</a>

    {{ include('candidat/_delete_form.html.twig') }}
{% endblock %}
