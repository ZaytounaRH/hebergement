{% extends 'base.html.twig' %}

{% block title %}Pointer le Départ{% endblock %}

{% block body %}
<div class="container mt-5">
    <h2 class="text-center mb-4">🛑 Pointer le départ</h2>

    <div class="card shadow-sm p-4">
        <p><strong>Employé :</strong> {{ presence.employee.user.nom }} {{ presence.employee.user.prenom }}</p>
        <p><strong>Date :</strong> {{ presence.date|date('d/m/Y') }}</p>
        <p><strong>Heure d'arrivée :</strong> {{ presence.heureArrive|date('H:i') }}</p>

        <form method="post" class="text-end mt-4">
            <button type="submit" class="btn btn-danger rounded-pill">
                ✅ Confirmer le départ
            </button>
        </form>
    </div>
</div>
{% endblock %}
